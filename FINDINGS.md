# Apple シリコンのファン制御と熱管理 — 実測でわかったこと

FanCurve を作る過程で調べた内容の記録。**使い方は [README.md](README.md)、こちらは調査結果**。

公開情報が誤っていた点が複数あるため、**どれが実測で、どれが推論で、どれが未検証か**を明示している。

**測定環境**: MacBook Pro (Mac15,10 / Apple M3 Max, 14 コア / GPU 30 コア) / macOS 26.6.2 / 2026-09-05〜06

> ここに書かれた数値は**この個体の実測値**。他の機種では取り直す必要がある（[README の該当節](README.md#別の-mac-で使う場合機種変github-から導入)）。

---

## 1. SMC のファン制御

### キー

| キー | 型 | 意味 |
|---|---|---|
| `FNum` | ui8 | ファン数（この機体は 2） |
| `F%dAc` | flt | 実回転数（読み取り専用） |
| `F%dMn` / `F%dMx` | flt | 最小 / 最大回転数。fan0 = 2,317–6,898、fan1 = 2,502–7,450 |
| `F%dTg` | flt | 目標回転数（**共有レジスタ**、後述） |
| `F%dMd` | ui8 | モード。**3 = 停止中、0 = 稼働中、1 = 手動** |
| `Ftst` | ui8 | 診断モードフラグ。手動制御の解除キー |

Intel Mac の `TC0P` / `TG0D` などは**存在しない**。

### `F%dMd` はモード指定ではなく状態表示

誰も書き込んでいない 900 秒の観測で、firmware 自身が 3 ↔ 0 を行き来させていた。

| 値 | 出現 | そのときのファン |
|---|---|---|
| 3 | 153 秒 | **0 rpm のみ** |
| 0 | 645 秒 | 0〜5,006 rpm |

**`Md=3` の間、`F%dTg` への書き込みは「受理」されるが完全に無視される**（ファンは回らない）。これを知らずに「書き込みは成功しているのに効かない」で長時間はまった。

### 手動制御に入る手順（M3 世代以降）

M3 世代から `thermalmonitord` がファンを mode 3 に保持し、mode キーへの書き込みを **status 0x82** で拒否する。

```
1. F%dMd = 1 を直接試す              ← M1 世代はこれで通るとされる
2. 拒否されたら Ftst = 1              ← 診断モード。thermalmonitord の回収を抑止
3. F%dMd が 3 を抜けるまでポーリング   ← 100 ms 間隔・最大 10 秒
4. F%dMd = 1 → F%dTg = <rpm>
5. 手放すときは Ftst = 0              ← 実測 2.9 秒で mode 3 に復帰
```

### `F%dTg` は共有レジスタ

**macOS 自身も書き込む。** 900 秒中 644 秒間、firmware が 2,317〜4,949 rpm を書いていた。
読めば「macOS の目標回転数」が分かる一方、**自分が書いた直後の読み戻しは信用できない**
（書き込み成功直後に 0 を返し、0 を書いた後に古い値を返す）。UI は読み戻しではなく
「自分が指示した値」を表示すべき。

### `Md=0` の間は書き込みが勝てない

macOS がファンを管理している最中（`Md=0`）に `Ftst` なしで `Tg` を書くと、**1 ms で上書きされる**。
200 Hz で書き続けても実回転はまったく動かない（全フェーズで「Tg が自分の値だった割合 0%」）。

### SMC ステータスコード

| コード | 意味 |
|---|---|
| `0x00` | 成功 |
| `0x82` | 書き込み拒否（mode 3 の状態で mode キーに書いた場合など） |
| `0x84` | キーが存在しない |
| `0x87` | エラーだが**値は書けていることがある**。成功扱いにしてよい |

### その他

- SMC の**読み取りは一般ユーザーで可能**。書き込みのみ root。SIP は enabled のままで書き込める
- `F%dMn` / `F%dMx` は目安であり、firmware は強制しない。クランプは呼び出し側の責任

---

## 2. macOS 標準のファン挙動（900 秒の実測）

制御ソフトを一切入れない状態で記録（`fancurvectl watch`）。

| 温度 | ファン |
|---|---|
| 〜80 °C | **0 rpm（停止）** |
| 80.0 °C | 回り始めるが**最低回転 2,317 rpm** |
| 80〜106 °C | **2,317 rpm に張り付き（333 秒間）** |
| 106.1 °C | ようやくランプ開始 |
| 117.6 °C（最高到達） | 5,006 rpm = **定格の 73%**。上限は使わない |

- 100 °C 超が 135 秒、110 °C 超が 80 秒
- スピンダウンも遅い。72.6 °C まで下がっても 4,940 rpm のまま
- **負荷開始から約 45 秒間ファンを回さない**。その間に die は 100 °C に達する

別の測定でもピークは 5,002 rpm で、**「定格の 73% で頭打ち」は再現性がある**。

---

## 3. 安全性 — ハードウェアに deadman はない

### `Ftst=1` のままプロセスが死んでも、macOS は回収しない

`Ftst=1` + `Md=1` + 5,000 rpm の状態で `SIGKILL` し、90 秒観測した結果:

```
kill 直後  Ftst=1  Md=1  Tg=5000  実回転 4,990 rpm
90 秒後    Ftst=1  Md=1  Tg=5000  実回転 5,007 rpm   ← 何も起きない
```

`Ftst=0` を書けば **2.9 秒**で mode 3 に戻る。復帰機構は正常だが、**それを実行する主体が死ぬと誰も肩代わりしない**。

Linux カーネルの [macsmc-hwmon](https://docs.kernel.org/hwmon/macsmc-hwmon.html) も手動ファン制御を
unsafe とし「**過熱時に安全側に倒れることを証明できない**」と明記している。
Macs Fan Control でも[同種の過熱事故](https://github.com/crystalidea/macs-fan-control/issues/387)が報告されている。

### スリープの挙動

| | 文献の主張 | 実測 |
|---|---|---|
| `Ftst` | 0 にリセットされる | **リセットされない（1 のまま）** |
| `F%dMd` | — | **3 に戻される** |
| `F%dTg` | — | **0 に消される** |

`Ftst` だけを見る自己検証はすり抜けるので、**ファンごとの mode も毎サイクル検証**する必要がある。

### launchd の再起動レイテンシ

`KeepAlive` デーモンを `SIGKILL` してから再起動までの実測:

| `ThrottleInterval` | レイテンシ |
|---|---|
| 未指定（既定 10 秒） | **11.13 / 2.12 / 6.14 秒**（前回の起動時刻から 10 秒の窓） |
| `1` | **0.12 秒**（4 回とも） |

本番デーモンで `Ftst` が 0 に戻るまでを測ると **1.192 秒**（Swift ランタイムのロード分が加わる）。

`ThrottleInterval=1` は速すぎて、**旧インスタンスが後片付けを終える前に新インスタンスが起動する**。
排他ロック（`flock`）で直列化が必要。

### 捕まえられる終了経路 / 捕まえられない経路

| 経路 | 猶予 | 捕捉 |
|---|---|---|
| 再起動・システム終了・ログアウト | launchd が `SIGTERM`、既定 20 秒待つ | ✅（実測 0.953 秒で解放） |
| スリープ | 電源通知 | ✅ |
| 電源ボタン長押し / カーネルパニック / 電源喪失 | なし | ❌ |

パニックは自動再起動するので、起動時に `Ftst=0` を書けば実質的に埋まる。

---

## 4. macOS の熱管理アーキテクチャ

`/usr/libexec/thermalmonitord` のバイナリを `strings` で調べた結果。

### `LifetimeServoController` = シリコンの摩耗モデル

```
%@: Saving TDDB Cluster Mask 0x%x to NAND
lts-ctrl-%u-%u-%u-is-tddb              ← コア別の積算器
LifetimeServoAFLUT                      ← Acceleration Factor のルックアップテーブル
LifetimeServoDieTempTarget
PCore_Die_Temp_Target / ECore_... / GCore_...
AppleCLPC
kAgingControllerDisabledKey
Failed saving LTS state to NVRam
LSControlLoop %d: tempMax %d, tempAverage %d, AFi %f, LS-ris %lld (up %lld, down %lld), target %d
```

**TDDB** = Time-Dependent Dielectric Breakdown（ゲート酸化膜の経時破壊）。温度に対して**指数的に加速**する半導体の摩耗機構。

macOS は:

1. コア種別（P / E / GPU）ごとに、温度から**劣化加速係数（AF）**を算出
2. それを**積算**し、**NVRAM と NAND に永続化**（OS 再インストールでも消えない）
3. 積算した消耗量に応じて**許容ダイ温度そのものを引き下げ**、`AppleCLPC` に送る

つまり「この個体があとどれだけ寿命を残しているか」を記録し、それに応じて制御を変えている。

### 積算器の読み方

NVRAM 変数 `lts-persistance`（64 バイト）:

```
offset 0:  uint32 version, counter, dieCount, loopCount
offset 16: double × 4   ← ループごとの積算器
```

この機体の例: `version=3 counter=8405 die=1 loop=4`、積算器 ≈ 1,632〜1,654 万。

**NVRAM への書き出しは非常に遅い。** 17 分の高負荷 + スリープを挟んでも `counter` が 1 も動かなかった。

### ライブの値を見る方法

```bash
sudo log config --subsystem com.apple.lifetimeservo --mode "level:debug"
log stream --predicate 'senderImagePath CONTAINS "thermalmonitord"' --level debug
sudo log config --subsystem com.apple.lifetimeservo --reset     # 必ず戻す
```

`die 0 loop N is: {length = 8, bytes = 0x...}` の行が出る。値は little-endian の double。

### ★ `Ftst=1` は寿命管理を止めない（実測）

アプリがファンを握った状態で観測した積算器の値を、NVRAM の保存値と比較:

| | NVRAM(保存) | ライブ | 差 |
|---|---|---|---|
| loop0 | 16,318,283 | 16,500,207 | **+181,924** |
| loop1 | 16,324,248 | 16,508,008 | **+183,760** |
| loop2 | 16,344,682 | 16,529,150 | **+184,468** |
| loop3 | 16,540,281 | 16,713,743 | **+173,462** |

**4 ループすべてで進んでいた。** `Ftst` が奪うのはファンの制御権だけで、
寿命管理も `AppleCLPC` のスロットリングもそのまま動き続ける。

### IORegistry

`lts-config`、`lts-clpc-voltage`、`AppleCLPC` の各種係数（時定数・電力上限）は読めるが、
**生のダイ温度ターゲットは出ていない**。静的な設定のみ。

---

## 5. A/B 実験 — 制御を奪うと何が変わるか

### 方法

Blender Cycles（GPU / 1920×1080 / 2048 samples / 13 フレーム）で**同一の計算量**を 2 回。
交絡を潰すため、両方とも「60 °C 未満・ファン停止」まで冷やしてから開始し、
スクリプトがレンダリングを起動した。ファン回転数は A の定常平均に B を合わせた（差 1 rpm）。

### 結果

| | A (macOS 標準) | B (アプリ制御) |
|---|---|---|
| 総時間 | 707.7 秒 | **658.2 秒（−7.0%）** |
| 最後の 5 フレーム平均 | **50.73 秒** | 59.03 秒（+16.4%） |
| 定常時の平均温度 | 83.5 °C | 89.6 °C |
| 最高温度 | **116.8 °C** | **112.1 °C** |
| 100 °C 以上の時間 | 161 秒 | **88 秒** |
| 105 °C 以上の時間 | 106 秒 | **7 秒** |
| 110 °C 以上の時間 | 40 秒 | **4 秒** |
| 定常時の平均電力 | 39.5 W | 46.6 W |

### 正しい解釈

**総時間の 7% 差は「リミッターの違い」ではない。** 内訳:

```
フレーム 1–3   A 44.3 / B 44.3 秒   同じ
フレーム 6–7   A 76.7, 79.1 秒 / B 44.4, 46.1 秒   ← ここで A が破綻
フレーム 9–13  A が速く、B が遅い
```

A は**開始 45 秒間ファンが 0 rpm**のまま電力 66 W を投入し、100 秒時点で 116.8 °C
（そのときファンはまだ 3,351 rpm）。その直後に強くスロットリングされ、
フレームが 76.7 / 79.1 秒まで落ちた。B はそれを回避した。

**定常状態では B のほうが遅い**（+16.4%）。総時間と符号が逆なので、
**この実験ではリミッターの挙動を分離できていない**。熱履歴が違うため。

### 分離できない理由

「同じ温度での電力」を比べる分析は**循環する**。温度は制御変数ではなく結果なので、
「絞られている → だから低温」と「絞られていない → だから高温」が同じ数字を生む。

分離するには、アプリ側で **macOS のファン立ち上がり（0 rpm を 45 秒 → ランプ）を再現**して
比較するしかない。未実施。

### 実務上の結論

- スロットリングは**両方で発生**した（電力 A 71→29 W、B 68→35 W）。`Ftst=1` は熱保護を無効化していない
- **熱ストレスはアプリ側が明確に小さい**。TDDB は温度に指数的なので、平均（89.7 / 89.9 °C でほぼ同じ）
  ではなく**高温側の裾**が効く。105 °C 超が 15 分の 1、110 °C 超が 10 分の 1
- ただし**この作業内容・このカーブでの 1 回の比較**であり、一般化には注意

---

## 6. 公開情報の誤り

[agoodkind/macos-smc-fan](https://github.com/agoodkind/macos-smc-fan) の `docs/research.md` は
`Ftst` 解除手順という核心を教えてくれた一方、挙動の記述は **3 件とも実測と食い違った**。

| 主張 | 実測 |
|---|---|
| スリープで firmware が `Ftst` を 0 にリセットする | されない（`Md`/`Tg` だけリセット） |
| クラッシュしても 250〜4000 ms で daemon が回収する | 90 秒経っても回収されない |
| `Ftst=1` が `LifetimeServoController` を抑止する | 抑止されていない（積算は進み続ける） |

**手順は信じる、挙動は測る。** この文書の数値も同じ扱いで、他機種では取り直すこと。

---

## 7. 温度センサー

`flt ` 型で妥当な値を返すものが **271 個**（`plausibleRange` 適用後は 229 個）。

| 接頭辞 | 分類 | 個数 |
|---|---|---|
| `Tp` `Te` | CPU（P/E コアクラスタ） | 106 |
| `Tg` | GPU | 32 |
| `TC` `TV` `TP` `TS` `TM` | SoC / 電源系 | 50 |
| `TH` `Th` | SSD | 15 |
| `Ts` `Ta` `TA` `TW` | 筐体・吸気・無線 | 25 |
| `TB*T` | バッテリー | 3 |

**注意点:**

- **`Tf**` は温度センサーではない。** 14 プロセスの CPU 負荷を 30 秒かけても 73.4 / 71.5 °C から
  1 ミリも動かない固定値だった
- **個別センサーは 0.5 秒で 15 °C 跳ねる**（アイドル時の実測）。制御判断に生値を使ってはいけない
- GPU レンダリング中でも**最も熱いのは CPU センサー**（`Tp`）だった（CPU 113.5 °C / GPU 107.0 °C）

---

## 8. 調査に使った道具

すべて `scripts/` と `fancurvectl` のサブコマンド。読み取り専用のものは root 不要。

| 道具 | 用途 | 書き込み |
|---|---|---|
| `fancurvectl watch` | 温度・回転数・電力・`Ftst` を 1 秒ごとに記録 | なし |
| `fancurvectl keys` | 任意の SMC キーを読む | なし |
| `fancurvectl dump` | 温度センサー一覧 | なし |
| `fancurvectl diag` | 制御方式を総当たり、SMC の生ステータス表示 | あり |
| `fancurvectl unlocktest` | `Ftst` 解除手順の実行と復帰の検証 | あり |
| `fancurvectl contest` | macOS が制御中に奪えるか | あり |
| `scripts/crashtest.sh` | `SIGKILL` 後に macOS が回収するか | あり |
| `scripts/restart-latency.sh` | launchd の再起動レイテンシ（SMC に触れない） | なし |
| `scripts/shutdown-test.sh` | 再起動・システム終了時の解放を検証 | あり |
| `scripts/sleeptest.sh` | スリープ前後の状態を記録 | なし |
| `scripts/ab-test.sh` + `ab-compare.py` | A/B 実験（Blender ベンチマーク込み） | あり |
| `scripts/lts-snapshot.py` | NVRAM の寿命積算状態を読む | なし |
| `scripts/lts-watch.sh` + `lts-decode-log.py` | 寿命サーボをライブ観測 | なし（ログ設定のみ・自動で戻す） |

---

## 9. 未解決の疑問

- **`LSControlLoop` の詳細行**（`tempMax` / `AFi` / `target` を含む）はログに出せていない。
  「寿命サーボが動いている」ことは確認できたが、**その温度ターゲットがアプリ制御時に変わるか**は未確認
- **`Ftst=1` がリミッターに影響するか**は A/B では分離できなかった。macOS のファン立ち上がりを
  再現した対照実験が必要
- NVRAM への書き出し契機と頻度が不明（17 分の負荷 + スリープでは書かれなかった）
- **M5 世代は未検証。** mode キーが小文字 `F%dmd` で `Ftst` が無いとされる
- `kAgingControllerDisabledKey` の実際の効果と設定方法
